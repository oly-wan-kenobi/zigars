interface:
  display_name: "zigars runtime crash forensics"
  short_description: "Turn Zig crashes into reproducible evidence."
  default_prompt: "Use $zigars-runtime-crash-forensics to analyze this Zig crash and preserve the raw evidence."
