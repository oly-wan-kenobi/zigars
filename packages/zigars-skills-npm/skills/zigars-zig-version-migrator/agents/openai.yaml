interface:
  display_name: "zigars Zig version migrator"
  short_description: "Migrate Zig projects across toolchain versions."
  default_prompt: "Use $zigars-zig-version-migrator to make this Zig version migration evidence-based and version-scoped."
