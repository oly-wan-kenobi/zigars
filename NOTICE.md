# Notices

zigar uses `mcp.zig` 0.0.4 as a Zig package URL dependency.

- Upstream project: <https://github.com/muhammad-fiaz/mcp.zig>
- Documentation: <https://muhammad-fiaz.github.io/mcp.zig/>
- License: MIT

`zig-pkg/` is treated as a local cache/artifact directory and is not intended to
be committed. The previous local compatibility patch for `mcp.zig` 0.0.3 is no
longer required by this project.
