| Filename | Claude's finding | Pushback reason |
|---|---|---|
| `2026-05-29-mcp-server-core-protocol-transports-review.md` | LOW-5: Configured workspace root appears in a resource error detail | The configured root is intentionally exposed elsewhere via `zigars://workspace` / workspace-roots outputs, and Claude admits it leaks nothing new. |
| `2026-05-29-npm-shim-mcpb-distribution-security-review.md` | LOW-3: `releaseBaseUrl` interpolates `version` / `repository` unencoded | Not a current defect. Both values are package-baked in the present call path, with no untrusted override. |
| `2026-05-29-npm-shim-mcpb-distribution-security-review.md` | LOW-4: `build.ts` `findTool` uses a shell string | The input is a static literal list in a private release-time package. Claude says impact is "none today." |
| `2026-05-29-app-usecases-validation-static-analysis-review.md` | #4: `plan()` embeds raw changed paths in `zig fmt` / `ast-check` argv | The path is first probed through `workspace_store.read`, so out-of-workspace paths are dropped. The argv is also only a returned plan, not a server-side spawn. |
| `2026-05-29-domain-manifest-bootstrap-review.md` | #4: `read_only=true` may coexist with execution capabilities | The raw field is not the MCP source of truth. `readOnlyHintFor` derives the external hint from risk flags, and `riskValue` exposes `mcp_read_only_hint`, not raw `read_only`. |
| `2026-05-29-domain-manifest-bootstrap-review.md` | #5: `idempotentHintFor` is dead logic | Redundant, but not false. Current behavior is a conservative "idempotent iff externally read-only" policy. |
| `2026-05-29-domain-manifest-bootstrap-review.md` | #7: comptime apply-gate checks a flag, not the schema | Claude states all current definitions include `apply`; this is a future invariant guard, not a live schema defect. |
| `2026-05-29-tools-build-ci-review.md` | MEDIUM-3: pure-Zig / no-`.py` gate can pass while stated property is violated | `docs/release.md` explicitly scopes the no-Python gate to `.github`, `docs`, `examples`, `scripts`, `src`, `tests`, and `tools`; npm packages are JS/TS by design. |
| `2026-05-29-tools-build-ci-review.md` | LOW-11: `workflow_dispatch` input interpolated into `run:` | This is an intentional manual setup hook for write-access actors, and Claude labels it accepted risk. |
| `2026-05-29-infra-zls-process-workspace-observability.md` | L2: `edits.zig` is dead code; its apply-gate / atomic-write invariant is unenforced | If it has no production caller, it cannot violate the source-mutating MCP invariant. This is dead-code cleanup, not a live apply-gate issue. |
