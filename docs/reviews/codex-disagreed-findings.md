# Codex-disagreed findings — adjudications

This table records findings that Codex (the second reviewer) pushed back on, and
the final adjudicated outcome applied across the review trackers. "Adjudication"
is the binding decision; severities in the source review files have been updated
to match.

| Filename | Claude's finding | Pushback reason | Adjudication |
|---|---|---|---|
| `2026-05-29-mcp-server-core-protocol-transports-review.md` | LOW-5: Configured workspace root appears in a resource error detail | The configured root is intentionally exposed elsewhere via `zigars://workspace` / workspace-roots outputs, and Claude admits it leaks nothing new. | Pushback upheld — not a defect. The root is already public surface; no new leak. |
| `2026-05-29-npm-shim-mcpb-distribution-security-review.md` | LOW-3: `releaseBaseUrl` interpolates `version` / `repository` unencoded | Not a current defect. Both values are package-baked in the present call path, with no untrusted override. | Pushback upheld — no current defect; package-baked inputs only. |
| `2026-05-29-npm-shim-mcpb-distribution-security-review.md` | LOW-4: `build.ts` `findTool` uses a shell string | The input is a static literal list in a private release-time package. Claude says impact is "none today." | Pushback upheld — no untrusted input; impact none today. |
| `2026-05-29-app-usecases-validation-static-analysis-review.md` | #4: `plan()` embeds raw changed paths in `zig fmt` / `ast-check` argv | The path is first probed through `workspace_store.read`, so out-of-workspace paths are dropped. The argv is also only a returned plan, not a server-side spawn. | **Codex partially WRONG.** The argv built in `plan()` (`src/app/usecases/validation/workflows.zig:478`) IS spawned server-side by `run()` (`workflows.zig:563`) — it is not merely a returned plan. The finding is nonetheless **not a defect** because the real protection is read-probe gating: every changed path is first probed through `context.workspace_store.read` (`workflows.zig:683`, `:904`, `:919`) so out-of-workspace paths are dropped before any spawn. Keep as informational/no-fix; correct the "just a plan" rationale to "safe via read-probe gating." |
| `2026-05-29-domain-manifest-bootstrap-review.md` | #4: `read_only=true` may coexist with execution capabilities | The raw field is not the MCP source of truth. `readOnlyHintFor` derives the external hint from risk flags, and `riskValue` exposes `mcp_read_only_hint`, not raw `read_only`. | Pushback upheld → reclassified **LOW**. Raw `meta.read_only` is internal; the MCP-facing `readOnlyHint` is derived by `readOnlyHintFor` (ANDs in all five `!`-risk flags, see `src/manifest/aggregate.zig:86`) and surfaced via `mcp_read_only_hint`. Coexistence of raw `read_only` with exec flags cannot mislead an MCP client. |
| `2026-05-29-domain-manifest-bootstrap-review.md` | #5: `idempotentHintFor` is dead logic | Redundant, but not false. Current behavior is a conservative "idempotent iff externally read-only" policy. | Pushback upheld — redundant but correct; conservative policy, no change required. |
| `2026-05-29-domain-manifest-bootstrap-review.md` | #7: comptime apply-gate checks a flag, not the schema | Claude states all current definitions include `apply`; this is a future invariant guard, not a live schema defect. | Pushback upheld — future-invariant guard, not a live defect. |
| `2026-05-29-tools-build-ci-review.md` | MEDIUM-3: pure-Zig / no-`.py` gate can pass while stated property is violated | `docs/release.md` explicitly scopes the no-Python gate to `.github`, `docs`, `examples`, `scripts`, `src`, `tests`, and `tools`; npm packages are JS/TS by design. | Pushback upheld → reclassified **LOW**. The gate is correctly scoped; the npm `packages/` tree is JS/TS by design and intentionally excluded. Closed by S14: tightened the gate's rejection message, scoped AGENTS.md/`docs/release.md` wording to the `.py`-extension behavior, and added a negative-path test that plants a `.py` under a scoped root and asserts the gate fails. |
| `2026-05-29-tools-build-ci-review.md` | LOW-11: `workflow_dispatch` input interpolated into `run:` | This is an intentional manual setup hook for write-access actors, and Claude labels it accepted risk. | Pushback upheld — **accepted risk**. Manual dispatch only, write-access actors, `contents: read`. Documented inline in the three workflows and in `docs/release.md` (S14). |
| `2026-05-29-infra-zls-process-workspace-observability.md` | L2: `edits.zig` is dead code; its apply-gate / atomic-write invariant is unenforced | If it has no production caller, it cannot violate the source-mutating MCP invariant. This is dead-code cleanup, not a live apply-gate issue. | Pushback upheld — dead-code cleanup, not a live apply-gate defect. |
